apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: myproject-pvc
  labels:
    app: myproject-pvc
spec:
  accessModes:
    - ReadWriteOnce 
  resources:
    requests:
      storage: 1Gi 
  storageClassName: default